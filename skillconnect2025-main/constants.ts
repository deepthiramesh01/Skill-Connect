
import { User, UserRole, Project, Skill, ProjectStatus, Pronouns, Rating, Announcement, Report, CollaborationRequest, Notification } from './types';

// This file is now much smaller as the initial data is stored in your Supabase database.
// You can create your first admin user directly in the Supabase table editor
// or by signing up through the app and changing the role in the database.
